<h1>Booking Review</h1>

<p><strong>Number of Tickets:</strong> <%= @booking.number_of_tickets %></p>
<p><strong>Total Amount:</strong> ₹<%= @booking.total_amount %></p>

<div>
  <%= link_to "Proceed to Payment", new_payment_path(booking_id: @booking.id), class: "btn btn-primary" %>
</div>

<%= link_to "Back", bookings_path, class: "btn btn-secondary" %>
